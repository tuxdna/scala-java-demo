Create a Java vs Scala language-comparison showcase in this repository.

TASK ONE

1. Enlist the essential features that compare Java and Scala (use matching topics in both languages).
2. Generate runnable code for each comparison case in Java and Scala.
3. Annotate each example with short, clear comments.
4. Create/update exactly two showcase files:
   - `JavaLanguageShowcase.java`
   - `ScalaLanguageShowcase.scala`
5. Add/update `README.md` with instructions to:
   - verify prerequisites (`javac`, `java`, `scalac`, `scala`)
   - compile and run both examples
   - optionally clean compiled artifacts
6. Keep the examples easy to run from the project root.


TASK TWO

Create another Scala file which demonstrates features not present in Java.

TASK THREE

Create another Java file which demonstrates features not present in Scala.

For above files, ensure that you have well documented demo code.

Also ensure that a makefile makes running examples and cleanup easy very convenient.

